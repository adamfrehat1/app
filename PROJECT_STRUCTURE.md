# 🏗️ بنية المشروع التفصيلية

## نظرة عامة

هذا الدليل يشرح بنية مشروع "مشتل" بالتفصيل

## الهيكل العام

```
app/
├── 📱 App.js                    # نقطة الدخول الرئيسية
├── 📄 app.json                  # إعدادات Expo
├── 📦 package.json              # المكتبات والتبعيات
├── ⚙️ babel.config.js           # إعدادات Babel
├── 🔐 .env.example              # نموذج متغيرات البيئة
├── 🚫 .gitignore                # ملفات محذوفة من Git
│
├── 📚 README.md                 # الوثائق الرئيسية
├── 🚀 QUICKSTART.md             # دليل البدء السريع
├── 🧪 TESTING.md                # دليل الاختبار
├── 🔥 FIREBASE_SETUP.md         # دليل إعداد Firebase
│
├── 🖼️ assets/                   # الصور والأيقونات
│   ├── icon.png                # أيقونة التطبيق (1024×1024)
│   ├── splash.png              # شاشة البداية
│   ├── adaptive-icon.png       # أيقونة Android
│   └── favicon.png             # أيقونة الويب
│
└── 📁 src/                      # الكود المصدري
    ├── config/                 # الإعدادات
    ├── services/               # الخدمات
    ├── screens/                # الشاشات
    ├── navigation/             # التنقل
    └── utils/                  # الأدوات المساعدة
```

## المجلدات الرئيسية

### 1. `src/config/` - الإعدادات

```
config/
└── firebase.js                 # إعدادات Firebase
```

**firebase.js**
- تهيئة Firebase
- تصدير خدمات Auth و Firestore و Storage
- قراءة المتغيرات من `.env`

### 2. `src/services/` - الخدمات

```
services/
├── authService.js              # خدمات المصادقة
└── productService.js           # خدمات المنتجات
```

**authService.js**
- `register()` - تسجيل مستخدم جديد
- `login()` - تسجيل الدخول
- `logout()` - تسجيل الخروج
- `getUserData()` - الحصول على بيانات المستخدم

**productService.js**
- `addProduct()` - إضافة منتج جديد
- `getAllProducts()` - الحصول على جميع المنتجات
- `getProductsByCategory()` - فلترة حسب الفئة
- `getProduct()` - الحصول على منتج واحد
- `searchProducts()` - البحث في المنتجات

### 3. `src/screens/` - الشاشات

```
screens/
├── LoginScreen.js              # شاشة تسجيل الدخول
├── RegisterScreen.js           # شاشة التسجيل
├── HomeScreen.js               # الشاشة الرئيسية
├── AddProductScreen.js         # إضافة منتج
├── ProductDetailsScreen.js     # تفاصيل المنتج
├── ChatScreen.js               # المحادثة
├── ExpertBookingScreen.js      # حجز الخبراء
├── FeedbackScreen.js           # الملاحظات والشكاوى
└── ProfileScreen.js            # الملف الشخصي
```

#### تفاصيل كل شاشة:

**LoginScreen.js**
- نموذج تسجيل الدخول
- التحقق من البيانات
- رابط للتسجيل
- دعم RTL

**RegisterScreen.js**
- نموذج إنشاء حساب
- التحقق من كلمة المرور
- حفظ البيانات في Firestore

**HomeScreen.js**
- عرض المنتجات في شبكة
- شريط البحث
- فلترة حسب الفئة
- زر إضافة منتج عائم (FAB)

**AddProductScreen.js**
- نموذج إضافة منتج
- رفع الصور المتعددة
- تحديد الموقع الجغرافي
- اختيار الفئة

**ProductDetailsScreen.js**
- عرض صور المنتج (متعددة)
- معلومات البائع
- الموقع على الخريطة
- أزرار التواصل

**ChatScreen.js**
- دردشة مباشرة
- إرسال واستقبال الرسائل
- عرض الوقت

**ExpertBookingScreen.js**
- قائمة الخبراء
- تقويم لاختيار التاريخ
- اختيار الوقت
- تأكيد الحجز

**FeedbackScreen.js**
- نموذج الملاحظات
- اختيار نوع الرسالة
- إرسال البريد الإلكتروني

**ProfileScreen.js**
- معلومات المستخدم
- قائمة المنتجات
- الإعدادات (الوضع الليلي، الإشعارات)
- تسجيل الخروج

### 4. `src/navigation/` - التنقل

```
navigation/
└── AppNavigator.js             # إعدادات التنقل
```

**AppNavigator.js**
- Stack Navigator للشاشات العامة
- Tab Navigator للشاشات الرئيسية
- التبديل بين Auth و Main Screens
- إعدادات RTL

**التبويبات السفلية:**
1. الرئيسية (Home)
2. الخبراء (ExpertBooking)
3. إضافة (AddProduct)
4. الملاحظات (Feedback)
5. الملف الشخصي (Profile)

### 5. `src/utils/` - الأدوات المساعدة

```
utils/
├── theme.js                    # ألوان وثيمات
└── constants.js                # الثوابت والبيانات
```

**theme.js**
- `lightTheme` - الوضع النهاري
- `darkTheme` - الوضع الليلي
- ألوان طبيعية (أخضر وبني)
- إعدادات React Native Paper

**constants.js**
- `CATEGORIES` - فئات المنتجات (4 فئات)
- `SAMPLE_PRODUCTS` - منتجات تجريبية (4 منتجات)
- `SAMPLE_EXPERTS` - خبراء تجريبيون (2 خبراء)

## تدفق البيانات

### 1. تسجيل الدخول/التسجيل
```
LoginScreen
    ↓
authService.login()
    ↓
Firebase Auth
    ↓
AppNavigator (يتحقق من user)
    ↓
TabNavigator (الشاشة الرئيسية)
```

### 2. إضافة منتج
```
AddProductScreen
    ↓
Image Picker (اختيار الصور)
    ↓
Location (الموقع)
    ↓
productService.addProduct()
    ↓
Firebase Storage (رفع الصور)
    ↓
Firestore (حفظ البيانات)
    ↓
HomeScreen (عرض المنتج)
```

### 3. عرض تفاصيل المنتج
```
HomeScreen (النقر على منتج)
    ↓
ProductDetailsScreen
    ↓
productService.getProduct()
    ↓
عرض البيانات + الخريطة
    ↓
ChatScreen (التواصل)
```

## قواعد البيانات (Firestore)

### Collections

1. **users**
   ```javascript
   {
     uid: string,
     name: string,
     email: string,
     phone: string,
     location: string,
     rating: number,
     totalRatings: number,
     isExpert: boolean,
     createdAt: timestamp
   }
   ```

2. **products**
   ```javascript
   {
     title: string,
     description: string,
     price: number,
     category: string,
     location: {
       latitude: number,
       longitude: number,
       address: string
     },
     images: [string],
     sellerId: string,
     sellerName: string,
     sellerRating: number,
     views: number,
     favorites: number,
     createdAt: timestamp
   }
   ```

3. **chats** (للمستقبل)
   ```javascript
   {
     participants: [userId1, userId2],
     messages: [{
       text: string,
       senderId: string,
       timestamp: timestamp
     }]
   }
   ```

## المكتبات المستخدمة

### Core
- **React Native** - إطار العمل الأساسي
- **Expo** - أدوات التطوير والبناء

### Navigation
- **React Navigation** - التنقل بين الشاشات
- **Stack Navigator** - للتنقل المتعدد المستويات
- **Bottom Tabs** - للتبويبات السفلية

### UI
- **React Native Paper** - مكونات Material Design
- **React Native Vector Icons** - الأيقونات

### Backend & Database
- **Firebase Auth** - المصادقة
- **Firebase Firestore** - قاعدة البيانات
- **Firebase Storage** - تخزين الصور

### Features
- **Expo Image Picker** - اختيار الصور
- **Expo Location** - الموقع الجغرافي
- **React Native Maps** - الخرائط
- **React Native Calendars** - التقويم

## نمط الكود

### JavaScript/ES6+
- استخدام `const` و `let` (لا `var`)
- Arrow functions
- Async/await للعمليات غير المتزامنة
- Destructuring
- Template literals

### React
- Functional Components
- React Hooks (useState, useEffect)
- Props & State management

### التنسيق
- تعليقات بالعربية للشرح
- أسماء متغيرات واضحة
- StyleSheet لكل component

## الأمان

### Best Practices
- ✅ استخدام `.env` للمفاتيح السرية
- ✅ عدم حفظ `.env` في Git
- ✅ قواعد Firestore لحماية البيانات
- ✅ التحقق من المدخلات (validation)
- ✅ استخدام HTTPS فقط

### قواعد Firestore
- المستخدم يقرأ ويكتب بياناته فقط
- الجميع يقرأ المنتجات
- المستخدمون المصرح لهم فقط يكتبون المنتجات

## الأداء

### التحسينات
- ✅ تحميل الصور بشكل تدريجي
- ✅ استخدام FlatList للقوائم الطويلة
- ✅ Pagination للمنتجات
- ✅ Caching للبيانات المتكررة

### Best Practices
- تجنب render غير الضروري
- استخدام useMemo و useCallback
- تحسين حجم الصور
- Lazy loading للمكونات

## التطوير المستقبلي

### ميزات مقترحة
- 🔔 نظام إشعارات Push
- 💬 دردشة فورية (Real-time)
- 💳 نظام الدفع
- 📊 لوحة تحكم للبائعين
- 🔍 بحث متقدم
- 🌟 نظام تقييم شامل
- 📱 دعم لغات إضافية
- 🎨 تخصيص الثيمات

### تحسينات تقنية
- State management (Redux/MobX)
- Unit tests
- E2E tests
- CI/CD pipeline
- Code splitting
- Performance monitoring

---

**ملاحظة**: هذا المشروع في مرحلة التطوير ويتم تحديثه باستمرار.
